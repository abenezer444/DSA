# Enter your code here. Read input from STDIN. Print output to STDOUT
n=int(input())
count={}
for i in range(n):
    word=input()
    count[word]=1+count.get(word,0)
print(len(count))
ans=[str(i) for i in count.values()]
print(" ".join(ans))
    
